# 排名算法
对于竞技类游戏而言，排名算法很重要，其不只是用于排名，还用于设计游戏的匹配机制。

本项目也用得到一点点匹配机制，在玩家多的情况下（虽然不太可能），优先把排名相近的玩家匹配到同一个Session。

## 技术选择
排名算法很多，例如经典的Elo算法，自带新手保护的Glicko/Glicko-2算法，微软的TrueSkill算法，开源的OpenSkill算法等等。

由于本项目玩法设计有*混战、中途进退、高并发填充、无队伍*等等特点，于是采用Glicko-2算法。